// ============================================================
// backend/Export.gs — EKSPOR DATA (placeholder server-side)
// Ekspor CSV/Excel/PDF dilakukan di sisi klien (browser).
// File ini disediakan untuk ekspor server-side di masa depan.
// ============================================================

// Placeholder — ekspor dilakukan di frontend (js/export.html)
function getExportData() {
  return getRecruitmentList();
}
